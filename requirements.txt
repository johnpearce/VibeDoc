# VibeDoc Agentapplication 核心 dependency
# 针 toMCP&Agent挑battle赛2025optimize的dependencypackage

# 🔧 核心 framework
gradio==5.34.1              # Agentinterface framework - 为use户提providedirect观的Agentexchange互bodyexperience
requests>=2.31.0            # HTTPplease requirement - use于MCPservice通trust
urllib3>=1.26.0
certifi>=2022.12.7

# 🤖 Agentconfiguration 管manage  
python-dotenv>=0.19.0
pydantic>=1.10.0
typing-extensions>=4.4.0

# 📊 Agentasynchronous process
aiofiles>=22.0
anyio>=3.0

# 🚀 Agentapplication server
fastapi>=0.115.2
uvicorn>=0.14.0

# 📝 Agentcontent process
markdown>=3.8.2             # Markdownprocess - Agentgenerate contentformatting

# 📋 多 format guideout support
python-docx>=1.2.0          # Worddocumentation guideout
reportlab>=4.4.3            # PDFgenerate support
html2text>=2024.4.24        # HTMLconvert

# Agent容devicetransform support (canselect)
# weasyprint>=57.0  # 需 want 额外 system dependency
# zipfile36>=0.1.3  # Python 3.6+ internal置 support